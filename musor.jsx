import React from 'react'

function musor({}) {
  function handleClick (5) {
    console.log(id)
  }
  return (
    <button onClick={handleClick}></button>
    {<button onClick={() => handleClick(5) }></button>}
  )
}

export default musor




